.order-list(layout='row')
  span(flex='')
  div(layout='column')
    h1 Order
    form.md-whiteframe-z1(ng-submit='save()', name='form', novalidate='', layout-padding='')
      p Add a order
      md-input-container
        label Name
        input(ng-model='newOrder.name', placeholder='Name')
      input.md-button(type='submit', value='Add')
    div(layout='row', layout-padding='')
      md-input-container(flex='65')
        label Search
        input(type='search', ng-model='search', placeholder='Search')
      md-select(ng-model='orderProperty', flex='35')
        md-option(value='1') Ascending
        md-option(value='-1') Descending

    ul
      li(dir-paginate='order in order | itemsPerPage: perPage', total-items='orderCount.count',  current-page='page')
        p 
          a(href='/order/{{order._id}}') {{ order.name }}
          button(ng-click='remove(order)') &times;
    dir-pagination-controls.md-accent(on-page-change='pageChanged(newPageNumber)')

    p Total number of order: {{orderCount.count}}
  span(flex='')